server:
  port: 8090
spring:
  application:
    name: spring-boot-admin-server
  boot:
    admin:
      #      context-path: /admin
      url: http://localhost:${server.port}
  jackson:
    serialization:
      indent_output: true
endpoints:
  health:
    sensitive: true

